import React from 'react';
import NavBar from './NavBar';

const Home = () => {
    return (
        <div>
            <NavBar />
            this is home
        </div>
    );
};

export default Home;