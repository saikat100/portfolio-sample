import React from 'react';
import NavBar from './NavBar';

const Post = () => {
    return (
        <div>
            <NavBar />
            this is post
        </div>
    );
};

export default Post;